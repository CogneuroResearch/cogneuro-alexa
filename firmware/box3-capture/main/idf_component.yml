dependencies:
  idf:
    version: ">=5.1"
  espressif/esp-box-3:
    version: "^2"
  espressif/esp_codec_dev:
    version: "^1.3"
  espressif/button:
    version: "^3"
  # Lets lwIP resolve cogneuro-pi.local, so the Pi does not need a static IP.
  # Requires CONFIG_LWIP_DNS_SUPPORT_MDNS_QUERIES, set in sdkconfig.defaults.
  espressif/mdns:
    version: "^1"
